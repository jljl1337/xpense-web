name: Test building the Docker image

on:
  pull_request:
    branches:
      - main

jobs:
  test:
    name: Test building the Docker image
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Build the Docker image
        run: docker build -t xpense-web:test .
